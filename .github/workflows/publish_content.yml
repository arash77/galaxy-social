name: Publish Content

on:
  pull_request:
    branches: [ "main" ]
    types:
      - closed

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v3

    - name: add processed_files
      run: |
        git fetch origin processed_files:processed_files
        git checkout processed_files -- processed_files.json

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: 3.9

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install markdown atproto Mastodon.py beautifulsoup4 pyyaml matrix-nio pillow slack_sdk

    - name: Run script to publish contents
      env:
        MASTODON_ACCESS_TOKEN: ${{ secrets.MASTODON_ACCESS_TOKEN }}
        MASTODON_API_BASE_URL: ${{ secrets.MASTODON_API_BASE_URL }}
        LINKEDIN_ACCESS_TOKEN: ${{ secrets.LINKEDIN_ACCESS_TOKEN }}
        BLUESKY_USERNAME: ${{ secrets.BLUESKY_USERNAME }}
        BLUESKY_PASSWORD: ${{ secrets.BLUESKY_PASSWORD }}
        BLUESKY_BASE_URL: ${{ secrets.BLUESKY_BASE_URL }}
        MATRIX_ACCESS_TOKEN: ${{ secrets.MATRIX_ACCESS_TOKEN }}
        MATRIX_ROOM_ID: ${{ secrets.MATRIX_ROOM_ID }}
        MATRIX_HOMESERVER: ${{ secrets.MATRIX_HOMESERVER }}
        MATRIX_USER_ID: ${{ secrets.MATRIX_USER_ID }}
        SLACK_ACCESS_TOKEN: ${{ secrets.SLACK_ACCESS_TOKEN }}
        SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
      run: python main.py

    - name: Commit changes
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        file_pattern: 'processed_files.json'
        branch: 'processed_files'